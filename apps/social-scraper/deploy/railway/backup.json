{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "deploy/Dockerfile.backup" },
  "deploy": {
    "cronSchedule": "0 2 * * *",
    "restartPolicyType": "NEVER"
  }
}
